## Dados

Os dados completos utilizados no projeto (ex: Olist) **não são versionados** por serem grandes.

Esta pasta pode conter:
- `sample/`: amostras pequenas para testes
- `raw/`: dados brutos locais (ignorados pelo git)

O fluxo principal do projeto funciona a partir do Excel operacional gerado via ETL.
